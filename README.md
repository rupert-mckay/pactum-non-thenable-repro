# Pactum Spec Non-Thenable Bug Repro

This repository exists to demonstrate an issue with the type declaration files provided by the library `pactum`.

## Reproduction steps

`npm install`

`npm run lint`
